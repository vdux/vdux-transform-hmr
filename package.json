{
  "name": "vdux-transform-hmr",
  "description": "Transform for HMR in vdux",
  "repository": "git://github.com/vdux/vdux-transform-hmr.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "global": "^4.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-core": "^6.5.2",
    "babel-preset-es2015": "^6.1.2",
    "babel-tape-runner": "^2.0.1",
    "tape": "^4.2.0"
  }
}
